# Repository publication rules

- Every worksheet PDF published to GitHub must be compiled with `\showsolutionsfalse` active.
- Before publishing `AllDays.pdf`, verify that every included day has an uncommented `\showsolutionsfalse` setting, then rebuild the combined PDF.
- Rebuild and verify every PDF after changing its solution visibility or title; never publish a stale build.
